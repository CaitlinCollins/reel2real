<div class="row">
    <div class="col-md-12">
        <h1>My Crate</h1>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        {{!-- This is the myRecords partial --}}
        {{#each records as |record|}}
        {{>myRecords}}
        {{/each}}
    </div>
</div>
